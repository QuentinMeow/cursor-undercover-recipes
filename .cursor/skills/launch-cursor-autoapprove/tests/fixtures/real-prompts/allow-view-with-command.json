{
  "name": "allow-view-with-command",
  "description": "Tool-call approval with command text visible in companion prompt",
  "source": "synthetic fixture for command extraction testing",
  "spec": {
    "role": "dialog",
    "modal": true,
    "content": "cat /etc/hosts | grep localhost",
    "buttons": [
      { "var": "a", "text": "View" },
      { "var": "b", "text": "Allow" }
    ]
  },
  "expect_click": true,
  "expect_id": "allow",
  "expect_single_click": true,
  "expect_command_preview": "cat /etc/hosts"
}
